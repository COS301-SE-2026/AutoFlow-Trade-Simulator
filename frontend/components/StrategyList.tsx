'use client';
import { StrategyCard } from '@/components/StrategyCard';

export function StrategyList() {

    return (
        <div>
            <StrategyCard />
            <StrategyCard />
            <StrategyCard />
            <StrategyCard />
        </div>
    );
}